import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('AboutPokemonPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AboutPokemonPage(title: 'AboutPokemon')));
    //  final titleFinder = find.text('AboutPokemon');
    //  expect(titleFinder, findsOneWidget);
  });
}
